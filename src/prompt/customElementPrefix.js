import { blue, gray, green } from "../util/colors.js";

export const customElementPrefixQ = `${blue}?:${gray} Enter custom element prefix (default: pwa): ${green}`;
export const customElementPrefixRegex = /^[a-z]{0,10}$/;
